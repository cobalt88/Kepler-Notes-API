/*
    ============SETTINGS ROUTE FILE============
    Path to here: {base_url}/api/settings/
    ==========================================
*/

import express from "express";
import Settings from "../models/Settings";

/* 
User settings and configuration routes. Create a new settings/configuration document - should be simple to start - create a default object. 
Update settings object - modify any fields within the Settings class object. 
Delete settings - Should likely only ever be used on user deletion. 
*/

export default router;
